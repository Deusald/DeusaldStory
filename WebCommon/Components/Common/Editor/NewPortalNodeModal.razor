@namespace DeusaldStoryWeb.Components.Common

@if (_Visible)
{
    <div class="modal-backdrop" @onclick="Cancel"></div>

    <div class="modal-box npm-portal-box" @onclick:stopPropagation="true">

        <div class="modal-header">
            <span class="modal-title">@(_IsEdit ? "Edit portal" : "New portal")</span>
            <button class="modal-close" @onclick="Cancel" title="Close">
                <i class="bi bi-x-lg"></i>
            </button>
        </div>

        <div class="modal-body">

            @if (!_IsEdit)
            {
                <div class="npm-portal-hint">
                    <i class="bi bi-box-arrow-in-right" style="color:var(--orange)"></i>
                    Creates a linked <strong>portal in</strong> and <strong>portal out</strong>. Flow reaching the in
                    teleports to the out — right-click either later to add more portal ins.
                </div>
            }

            @* ── Name ──────────────────────────────────────────────────── *@
            <div class="modal-field">
                <label class="modal-label">Name</label>
                <input class="modal-input"
                       type="text"
                       placeholder="To the market"
                       @bind="_Name"
                       @bind:event="oninput"
                       maxlength="100"/>
            </div>

            @* ── Description ───────────────────────────────────────────── *@
            <div class="modal-field">
                <label class="modal-label">
                    Description
                    <span class="modal-label-hint">optional</span>
                </label>
                <textarea class="modal-input npm-portal-textarea"
                          placeholder="A short note about where this portal leads…"
                          @bind="_Description"
                          @bind:event="oninput"
                          rows="2"
                          maxlength="500"></textarea>
            </div>

        </div>

        <div class="modal-footer">
            <button class="modal-btn modal-btn-ghost" @onclick="Cancel">Cancel</button>
            <button class="modal-btn modal-btn-primary"
                    @onclick="Confirm"
                    disabled="@(!_CanConfirm)">
                @(_IsEdit ? "Save changes" : "Create portal")
            </button>
        </div>

    </div>
}

@code {
    [Parameter] public EventCallback<NewPortalNodeArgs> OnConfirm { get; set; }

    private bool   _Visible;
    private bool   _IsEdit;
    private Guid   _EditId;
    private string _Name        = "";
    private string _Description = "";

    /// <summary>Opens the modal to create a brand-new portal pair.</summary>
    public void Show()
    {
        _IsEdit      = false;
        _EditId      = Guid.Empty;
        _Name        = "";
        _Description = "";
        _Visible     = true;
        StateHasChanged();
    }

    /// <summary>Opens the modal to rename / re-describe an existing portal pair.</summary>
    public void ShowEdit(Guid id, string name, string description)
    {
        _IsEdit      = true;
        _EditId      = id;
        _Name        = name;
        _Description = description;
        _Visible     = true;
        StateHasChanged();
    }

    private bool _CanConfirm => !string.IsNullOrWhiteSpace(_Name);

    private async Task Confirm()
    {
        if (!_CanConfirm) return;
        _Visible = false;
        await OnConfirm.InvokeAsync(new NewPortalNodeArgs(_EditId, _Name.Trim(), _Description.Trim()));
    }

    private void Cancel() => _Visible = false;

    /// <summary>Result passed back to the parent. <see cref="EditId"/> is empty for a create, set for an edit.</summary>
    public record NewPortalNodeArgs(Guid EditId, string Name, string Description);
}
